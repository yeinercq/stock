<% available_events_for(invoice).each do |event| %>
  <%= link_to event, trigger_invoice_path(invoice, event: event), data: { turbo_method: :patch, turbo_confirm: "Are you sure?" }, class: 'dropdown-item' %>
<% end %>
